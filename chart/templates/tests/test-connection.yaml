apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "k8svpn.fullname" . }}-test-connection"
  labels:
    {{- include "k8svpn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "k8svpn.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
